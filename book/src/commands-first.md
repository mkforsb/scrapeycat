# The `first` command

```lua
first()
```

The `first` command discards all results except for the first one.

## Examples

```lua
-- results = ["Alice", "Bob", "Charlie"]

first()

-- results = ["Alice"]
```
